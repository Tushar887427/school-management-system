# Keystore Configuration for Local Android APK Builds
# 
# IMPORTANT: This is an EXAMPLE file. Do NOT commit the actual keystore.properties
# file with real credentials to your repository!
#
# To use this file for local builds:
# 1. Copy this file to keystore.properties (without .example)
# 2. Fill in your actual keystore details
# 3. Make sure keystore.properties is in .gitignore
#
# The setup-cordova.sh script will automatically load these values
# when building signed release APKs locally.

# Path to your keystore file (use absolute path for reliability)
# Example: /home/username/android-keys/school-management.keystore
# Example (Windows): C:/Users/username/android-keys/school-management.keystore
KEYSTORE_PATH=/path/to/your/keystore.jks

# Keystore password (the password you set when creating the keystore)
KEYSTORE_PASSWORD=your_keystore_password_here

# Key alias (the alias you specified when creating the keystore)
KEY_ALIAS=your_key_alias_here

# Key password (the password for the specific key)
KEY_PASSWORD=your_key_password_here

# Note: If you don't have a keystore yet, generate one with:
# keytool -genkey -v -keystore school-management.keystore \
#   -alias school-mgmt-key \
#   -keyalg RSA \
#   -keysize 2048 \
#   -validity 10000
